# quickstart-agent

The Deep Agent built in `docs/00-quickstart.md`: one file of agent code, one `langgraph.json`,
one `.env`. Served locally with `uv run langgraph dev`, then as an image under
`../docker-compose.split.yml` with a separate worker container.
